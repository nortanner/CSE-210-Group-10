from game.points import Points
from game.moving import Moving

class Director:
    def __init__(self, player_name):
        self.player = player_name
        current_points = Points()
    def hit_object():
        pass
    def game_play():
        if Director.hit_object() == True:
            current_points = Points.update_points()
        

